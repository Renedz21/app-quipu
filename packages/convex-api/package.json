{
  "name": "@quipu/convex-api",
  "version": "0.0.0",
  "private": true,
  "description": "Convex API spec (functions + components) for non-web consumers (mobile, scripts, etc). Generated from apps/web/convex by `convex-helpers ts-api-spec`.",
  "main": "./src/api.ts",
  "types": "./src/api.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "generate": "cd ../../apps/web && pnpm convex:export-api",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/ts-config": "workspace:*",
    "convex": "^1.43.0",
    "typescript": "6.0.3"
  }
}
